# 复制为 .env 后填写实际值；.env 已被 Git 忽略。

# local 或 external。
MUMBLE_AUTH_MODE=local

# external 模式下填写身份提供者地址与稳定实例标识。
MUMBLE_EXTERNAL_AUTH_URL=
MUMBLE_EXTERNAL_AUTH_SERVER_INSTANCE_ID=voice-server-01

# 以下令牌仅保存在部署环境中，不要提交到版本库。
MUMBLE_EXTERNAL_AUTH_SERVICE_TOKEN=
MUMBLE_IDENTITY_REVALIDATE_TOKEN=

